function [area] = getRectangleArea(S, x,y, dx,dy,h,w)
    x1 = S(y+dx+1,x+dy+1);
    x2 = S(y+dx+1,x+dy+w);
    x3 = S(y+dx+h,x+dy+1);
    x4 = S(y+dx+h,x+dy+w);
    
    S(y+2*h1+d1,x+w1+d2)

    area = x4-x2-x3+x1;
end